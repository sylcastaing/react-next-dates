import { DatePicker } from '../src';
import { ArgsTable, Canvas, CodeOrSourceMdx, Meta, Preview, Source, Story } from '@storybook/addon-docs/blocks';

import { enUS } from 'date-fns/locale';

import './date-picker.scss';
import { useState } from 'react';

<Meta
  title="DatePicker"
  component={DatePicker}
  argTypes={{
    locale: {
      name: 'locale',
      description: 'Date fns locale',
      table: {
        type: { summary: 'Locale' },
      },
    },
  }}
/>

# Props

<ArgsTable of="." />

# Default

<Canvas>
  <Story name="Default">
    {() => {
      const [date, setDate] = useState(null);
      return (
        <div className="date-picker-container">
          <DatePicker locale={enUS} date={date} onChange={setDate} portalContainer={document.body}>
            {({ inputProps }) => <input {...inputProps} />}
          </DatePicker>
        </div>
      );
    }}
  </Story>
</Canvas>

# Month

<Canvas>
  <Story name="Month">
    {() => {
      const [date, setDate] = useState(null);
      return (
        <div className="date-picker-container">
          <DatePicker locale={enUS} type="month" format="MM/yyyy" date={date} onChange={setDate} portalContainer={document.body}>
            {({ inputProps }) => <input {...inputProps} />}
          </DatePicker>
        </div>
      );
    }}
  </Story>
</Canvas>
