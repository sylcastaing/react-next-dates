import { DefaultModifiers } from './index';

export const DEFAULT_MODIFIERS_CLASS_NAMES: Record<DefaultModifiers, string> = {
  disabled: '-disabled',
  selected: '-selected',
  today: '-today',
  outside: '-outside',
};
